/** @type {import('next').NextConfig} */
const nextConfig = {
  //email verify token deleted cause of useEffect working twice
  //set true in dev
  reactStrictMode: false,
};

export default nextConfig;
